---
phase: 03-perception-chain-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tiptop/tiptop/perception/sam.py
  - tiptop/tiptop/perception_wrapper.py
  - tiptop/tests/test_perception_baseline.py
autonomous: true
requirements:
  - PERC-01
  - PERC-03
user_setup: []
must_haves:
  truths:
    - The main TiPToP segmentation baseline is explicitly protected as SAM3-first.
    - Legacy SAM2 remains available only as an explicit compatibility mode.
    - The hybrid `use_vlm_text_prompts` branch is treated as real baseline behavior, not incidental local state.
  artifacts:
    - tiptop/tiptop/perception/sam.py
    - tiptop/tiptop/perception_wrapper.py
    - tiptop/tests/test_perception_baseline.py
  key_links:
    - `sam.py` should remain the narrow runtime control point for backend selection.
    - `perception_wrapper.py` should expose the current SAM3 text-prompt branch clearly enough to test.
---

<objective>
Audit and tighten SAM3 baseline integration points.

Purpose: Make the real TiPToP segmentation path clearly SAM3-first and protect the hybrid text-prompt branch that the validated workspace already uses.
Output: Tightened runtime semantics plus focused tests for baseline selection.
</objective>

<context>
@.planning/phases/03-perception-chain-stabilization/03-CONTEXT.md
@.planning/phases/03-perception-chain-stabilization/03-RESEARCH.md
@.planning/phases/03-perception-chain-stabilization/03-VALIDATION.md
@tiptop/tiptop/perception/sam.py
@tiptop/tiptop/perception_wrapper.py
@tiptop/tiptop/perception/sam3.py
@tiptop/tiptop/config/tiptop.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten and clarify SAM3-first runtime selection</name>
  <files>tiptop/tiptop/perception/sam.py</files>
  <action>Review backend selection semantics and warning paths so `sam3` remains the explicit default and legacy `sam2` behavior stays clearly compatibility-only. Tighten code or messages only where it improves baseline clarity without expanding scope.</action>
  <verify>bash -lc 'cd tiptop && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pixi run python -m pytest tests/test_perception_baseline.py -q'</verify>
  <done>The segmentation selector clearly protects SAM3 as the default TiPToP baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused coverage for the SAM3 hybrid text-prompt branch</name>
  <files>tiptop/tiptop/perception_wrapper.py, tiptop/tests/test_perception_baseline.py</files>
  <action>Add deterministic focused tests around the `detect_and_segment()` decision path so the `sam3 + use_vlm_text_prompts` branch and its fallback behavior are explicitly protected. Small refactors are allowed if they make this branch easier to test.</action>
  <verify>bash -lc 'cd tiptop && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pixi run python -m pytest tests/test_perception_baseline.py -q'</verify>
  <done>The active SAM3 text-prompt behavior is covered by focused tests and easier to reason about.</done>
</task>

</tasks>

<verification>
- [ ] `bash -lc 'cd tiptop && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pixi run python -m pytest tests/test_perception_baseline.py -q'`
- [ ] `bash -lc 'rg -n "sam3|sam2|use_vlm_text_prompts|Ignoring VLM bounding boxes" tiptop/tiptop/perception/sam.py tiptop/tiptop/perception_wrapper.py'`
</verification>

<output>
After completion, create `.planning/phases/03-perception-chain-stabilization/03-01-SUMMARY.md`
</output>
