---
phase: 02-config-and-bring-up-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tiptop/tiptop/config/__init__.py
  - tiptop/tiptop/perception/sam3.py
  - tiptop/tiptop/config/tiptop.yml
  - tiptop/tests/test_workspace_config.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
user_setup: []
must_haves:
  truths:
    - Runtime code has one centralized way to resolve the workspace root and sibling repo defaults.
    - SAM3 path and checkpoint resolution no longer depend on one irreducible `/home/user/tiptop/...` fallback path.
    - The validated local workstation still resolves to the same active sibling repos by default.
  artifacts:
    - tiptop/tiptop/config/__init__.py
    - tiptop/tiptop/perception/sam3.py
    - tiptop/tests/test_workspace_config.py
  key_links:
    - Workspace-root resolution in `tiptop.config` must feed the SAM3 local path logic.
    - Tests must cover both explicit override precedence and validated local fallback behavior.
---

<objective>
Centralize runtime workspace-root and sibling-repo resolution.

Purpose: Remove hidden path coupling from active runtime code while preserving the current validated workstation baseline.
Output: Shared path-resolution helpers plus focused test coverage.
</objective>

<context>
@.planning/phases/02-config-and-bring-up-hardening/02-CONTEXT.md
@.planning/phases/02-config-and-bring-up-hardening/02-RESEARCH.md
@.planning/phases/02-config-and-bring-up-hardening/02-VALIDATION.md
@tiptop/tiptop/config/__init__.py
@tiptop/tiptop/perception/sam3.py
@tiptop/tiptop/config/tiptop.yml
@tiptop/tests/test_urinal_localization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized workspace-root and sibling-path helpers to `tiptop.config`</name>
  <files>tiptop/tiptop/config/__init__.py</files>
  <action>Add helper functions that resolve the workspace root and sibling repo defaults using a layered precedence model such as env -> config -> inferred package-relative root. Keep existing `tiptop_cfg()` behavior intact. The helpers should be reusable by runtime code and tests without requiring CLI entrypoints.</action>
  <verify>bash -lc 'cd tiptop && python -m pytest tests/test_workspace_config.py -q'</verify>
  <done>Central workspace-root resolution exists and is testable.</done>
</task>

<task type="auto">
  <name>Task 2: Route SAM3 local path and checkpoint logic through the shared resolver</name>
  <files>tiptop/tiptop/perception/sam3.py, tiptop/tiptop/config/tiptop.yml</files>
  <action>Update `sam3.py` so project-root/checkpoint fallback behavior uses the shared config helper instead of hard-coding `/home/user/tiptop/sam3`. Adjust config comments or defaults only as needed to clarify that current values are validated local defaults, not universal absolute assumptions.</action>
  <verify>bash -lc 'cd tiptop && python -m pytest tests/test_workspace_config.py -q'</verify>
  <done>SAM3 resolution honors override precedence and derived workspace defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for workspace-root and SAM3 resolution precedence</name>
  <files>tiptop/tests/test_workspace_config.py</files>
  <action>Create a new focused unit test module that covers explicit env-var overrides, profile/config-based overrides, inferred local fallback behavior, and SAM3 checkpoint resolution behavior. Keep tests filesystem-local and deterministic.</action>
  <verify>bash -lc 'cd tiptop && python -m pytest tests/test_workspace_config.py -q'</verify>
  <done>Focused path-resolution coverage protects the Phase 2 centralization work.</done>
</task>

</tasks>

<verification>
- [ ] `bash -lc 'cd tiptop && python -m pytest tests/test_workspace_config.py -q'`
- [ ] `bash -lc 'rg -n '\"/home/user/tiptop/sam3\"|\"/home/user/tiptop/Fast-FoundationStereo\"' tiptop/tiptop/perception/sam3.py tiptop/tiptop/config/__init__.py'`
</verification>

<output>
After completion, create `.planning/phases/02-config-and-bring-up-hardening/02-01-SUMMARY.md`
</output>
