---
phase: 01-workspace-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/WORKSPACE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - WKSP-01
  - WKSP-03
user_setup: []
must_haves:
  truths:
    - Workspace planning artifacts classify the workspace root into active repos, reference repos, generated artifacts, and local archives.
    - Planning artifacts name `tiptop/`, `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, and `droid-sim-evals/` as the active baseline while keeping `FoundationStereo/` reference-only.
    - Planning artifacts explain how the workspace-root view connects to TiPToP runtime entrypoints and sibling services.
  artifacts:
    - .planning/codebase/WORKSPACE.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
  key_links:
    - .planning/codebase/WORKSPACE.md must reuse the same active/reference/generated/archive classification language as .planning/codebase/STRUCTURE.md.
    - .planning/codebase/WORKSPACE.md and .planning/codebase/ARCHITECTURE.md must both connect the active baseline repos to `tiptop-run`, `tiptop-h5`, and `tiptop-server`.
---

<objective>
Create the planning-side source of truth for the real workspace boundary.

Purpose: Make downstream plans and future contributors reason from `/home/user/tiptop` instead of from `tiptop/` alone.
Output: A dedicated workspace map plus aligned structure and architecture planning docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-baseline/01-CONTEXT.md
@.planning/phases/01-workspace-baseline/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a dedicated workspace boundary map in `.planning/codebase/WORKSPACE.md`</name>
  <files>.planning/codebase/WORKSPACE.md</files>
  <read_first>.planning/phases/01-workspace-baseline/01-CONTEXT.md, .planning/phases/01-workspace-baseline/01-RESEARCH.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `.planning/codebase/WORKSPACE.md` as the planning-side source of truth for the workspace root. Include these exact sections: `## Workspace Layers`, `## Active Baseline`, `## Reference and Local-Only Items`, `## Runtime Dependency View`, and `## Human Entry Surfaces`. In `## Workspace Layers`, classify the root into four explicit buckets: active repos (`tiptop/`, `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, `droid-sim-evals/`), reference repo (`FoundationStereo/`), generated artifacts (`d435_probe_outputs/`, `sam3_*_outputs/`, `tmp_*`), and local archives (`*.zip`). In `## Runtime Dependency View`, state that `tiptop/` orchestrates the stack while `Fast-FoundationStereo/` and `M2T2/` provide local services, `sam3/` is a local code dependency, and `droid-sim-evals/` supports offline/websocket evaluation. Call out that `FoundationStereo/` is retained for reference only.</action>
  <verify>bash -lc 'test -f .planning/codebase/WORKSPACE.md && rg -n "## Workspace Layers|## Active Baseline|## Runtime Dependency View|FoundationStereo/|Fast-FoundationStereo/|droid-sim-evals/" .planning/codebase/WORKSPACE.md'</verify>
  <acceptance_criteria>
    - `.planning/codebase/WORKSPACE.md` exists
    - `.planning/codebase/WORKSPACE.md` contains `## Workspace Layers`
    - `.planning/codebase/WORKSPACE.md` contains the exact repo names `tiptop/`, `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, `droid-sim-evals/`, and `FoundationStereo/`
    - `.planning/codebase/WORKSPACE.md` contains the phrases `Generated artifacts` and `Local archives`
  </acceptance_criteria>
  <done>Workspace map exists and encodes the four-layer classification plus active baseline/runtime dependency view.</done>
</task>

<task type="auto">
  <name>Task 2: Align `.planning/codebase/STRUCTURE.md` with the four-layer root classification</name>
  <files>.planning/codebase/STRUCTURE.md</files>
  <read_first>.planning/codebase/STRUCTURE.md, .planning/codebase/WORKSPACE.md, .planning/phases/01-workspace-baseline/01-CONTEXT.md</read_first>
  <action>Update `.planning/codebase/STRUCTURE.md` so the root directory layout and directory-purposes sections explicitly reflect the four-layer classification from `WORKSPACE.md`. Preserve existing useful details, but revise the root tree comments and the root-level directory purpose prose so `FoundationStereo/` is clearly marked reference-only, `d435_probe_outputs/`, `sam3_*_outputs/`, and `tmp_scene4_frames/` are clearly marked generated/debug artifacts, and root `*.zip` bundles are clearly marked local archives. Do not remove the existing active-repo descriptions for `tiptop/`, `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, and `droid-sim-evals/`.</action>
  <verify>bash -lc 'rg -n "Reference repo|generated/debug artifacts|local archives|Fast-FoundationStereo|FoundationStereo|d435_probe_outputs" .planning/codebase/STRUCTURE.md'</verify>
  <acceptance_criteria>
    - `.planning/codebase/STRUCTURE.md` contains `Fast-FoundationStereo/`
    - `.planning/codebase/STRUCTURE.md` contains `FoundationStereo/` together with `reference`
    - `.planning/codebase/STRUCTURE.md` contains `d435_probe_outputs/` together with `generated` or `debug`
    - `.planning/codebase/STRUCTURE.md` contains `*.zip` or `zip` together with `archive`
  </acceptance_criteria>
  <done>The structure map now mirrors the same boundary model as `WORKSPACE.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Update `.planning/codebase/ARCHITECTURE.md` to describe the active workspace boundary from the root</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/codebase/WORKSPACE.md, tiptop/docs/command-reference.md, tiptop/docs/development-build.md</read_first>
  <action>Revise the `Workspace Integration Layer`, `Pattern Overview`, and `Entry Points` sections of `.planning/codebase/ARCHITECTURE.md` so they explicitly describe the active baseline as `tiptop/` plus sibling repos `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, and `droid-sim-evals/`, with `FoundationStereo/` retained only as a reference checkout. Keep the current layered runtime description, but ensure the architecture doc names `tiptop-run`, `tiptop-h5`, and `tiptop-server` as TiPToP entrypoints that depend on the workspace-root boundary being understood correctly.</action>
  <verify>bash -lc 'rg -n "Workspace Integration Layer|tiptop-run|tiptop-h5|tiptop-server|FoundationStereo/.*reference|Fast-FoundationStereo|M2T2|droid-sim-evals" .planning/codebase/ARCHITECTURE.md'</verify>
  <acceptance_criteria>
    - `.planning/codebase/ARCHITECTURE.md` contains `Workspace Integration Layer`
    - `.planning/codebase/ARCHITECTURE.md` contains `tiptop-run`, `tiptop-h5`, and `tiptop-server`
    - `.planning/codebase/ARCHITECTURE.md` contains `FoundationStereo/` and `reference`
    - `.planning/codebase/ARCHITECTURE.md` contains `Fast-FoundationStereo`, `M2T2`, and `droid-sim-evals`
  </acceptance_criteria>
  <done>The architecture doc now explains the active workspace boundary and its connection to runtime entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor or planner -> workspace planning docs | Humans and downstream agents trust these docs to decide what belongs to the active system |
| Planning docs -> runtime stack understanding | Incorrect repo/service classification can misdirect future bring-up and change planning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `.planning/codebase/WORKSPACE.md` and related maps | mitigate | Copy the active baseline repo list exactly from `01-CONTEXT.md`, and keep `FoundationStereo/` explicitly marked reference-only in all planning docs touched by this plan |
| T-01-02 | I | Root classification for generated outputs and archives | mitigate | Explicitly mark `d435_probe_outputs/`, `sam3_*_outputs/`, `tmp_*`, and root `*.zip` bundles as generated/local-only so future plans do not treat them as source of truth |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bash -lc 'test -f .planning/codebase/WORKSPACE.md'`
- [ ] `bash -lc 'rg -n "Active repos|Reference repo|Generated artifacts|Local archives" .planning/codebase/WORKSPACE.md .planning/codebase/STRUCTURE.md >/dev/null'`
- [ ] `bash -lc 'rg -n "tiptop-run|tiptop-h5|tiptop-server" .planning/codebase/ARCHITECTURE.md >/dev/null'`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Workspace planning docs now encode the root boundary without treating `tiptop/` as the full project
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-baseline/01-01-SUMMARY.md`
</output>
