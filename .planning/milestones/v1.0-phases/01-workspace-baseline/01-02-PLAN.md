---
phase: 01-workspace-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - README.md
  - AGENTS.md
  - tiptop/README.md
autonomous: true
requirements:
  - WKSP-01
  - WKSP-03
user_setup: []
must_haves:
  truths:
    - `/home/user/tiptop/README.md` exists and introduces the workspace as a multi-repo system rooted at `/home/user/tiptop`.
    - `AGENTS.md` and `tiptop/README.md` no longer imply that `tiptop/` alone defines the active system.
    - The root entrypoint routes readers into deeper TiPToP docs instead of duplicating the full command manual.
  artifacts:
    - README.md
    - AGENTS.md
    - tiptop/README.md
  key_links:
    - `README.md` must point readers to `.planning/codebase/WORKSPACE.md` and the deeper TiPToP docs.
    - `tiptop/README.md` must mention the workspace-root entrypoint for the validated local stack.
---

<objective>
Align the human-facing guidance files with the real workspace boundary.

Purpose: Give contributors and agents one consistent story about where the project starts and how the TiPToP repo fits inside the larger stack.
Output: A new root README plus updated guidance in `AGENTS.md` and `tiptop/README.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-baseline/01-CONTEXT.md
@.planning/phases/01-workspace-baseline/01-RESEARCH.md
@.planning/codebase/WORKSPACE.md
@AGENTS.md
@tiptop/README.md
@tiptop/docs/getting-started.md
@tiptop/docs/development-build.md
@tiptop/docs/command-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the workspace-root `README.md` as the canonical human entrypoint</name>
  <files>README.md</files>
  <read_first>.planning/phases/01-workspace-baseline/01-CONTEXT.md, .planning/phases/01-workspace-baseline/01-RESEARCH.md, .planning/codebase/WORKSPACE.md, AGENTS.md, tiptop/README.md, tiptop/docs/getting-started.md, tiptop/docs/development-build.md</read_first>
  <action>Create `/home/user/tiptop/README.md` with an explicit workspace-root introduction. Include these exact sections: `## Start Here`, `## Active Baseline Repos`, `## What Is Not Part of the Active Baseline`, and `## Where To Go Next`. In `## Active Baseline Repos`, list `tiptop/`, `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, and `droid-sim-evals/`. In `## What Is Not Part of the Active Baseline`, call out `FoundationStereo/` as reference-only plus generated artifact folders and root-level archives as local-only. In `## Where To Go Next`, link to `.planning/codebase/WORKSPACE.md`, `tiptop/docs/getting-started.md`, `tiptop/docs/development-build.md`, and `tiptop/docs/command-reference.md`. Do not duplicate full command sequences from TiPToP docs.</action>
  <verify>bash -lc 'test -f README.md && rg -n "## Start Here|## Active Baseline Repos|## What Is Not Part of the Active Baseline|tiptop/docs/getting-started.md|tiptop/docs/development-build.md" README.md'</verify>
  <acceptance_criteria>
    - `README.md` exists
    - `README.md` contains `## Start Here`
    - `README.md` contains all five active baseline repo names
    - `README.md` contains `FoundationStereo/` together with `reference`
    - `README.md` contains links to `tiptop/docs/getting-started.md` and `tiptop/docs/development-build.md`
  </acceptance_criteria>
  <done>The workspace root now has a canonical onboarding entrypoint that frames the real system boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Update `AGENTS.md` so agent guidance matches the new root entrypoint</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md, README.md, .planning/codebase/WORKSPACE.md, .planning/phases/01-workspace-baseline/01-CONTEXT.md</read_first>
  <action>Revise `AGENTS.md` so its project description and architecture summary explicitly reference the new root `README.md` and the workspace-root boundary. Preserve the existing GSD sections, but ensure the Project, Architecture, and Workflow portions say that `/home/user/tiptop` is the project boundary and that `README.md` is the canonical human entrypoint while `.planning/codebase/WORKSPACE.md` is the planning-side source of truth.</action>
  <verify>bash -lc 'rg -n "README.md|workspace root|/home/user/tiptop|multi-repo" AGENTS.md'</verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `README.md`
    - `AGENTS.md` contains `/home/user/tiptop`
    - `AGENTS.md` contains `multi-repo`
    - `AGENTS.md` does not describe `tiptop/` as the full project boundary
  </acceptance_criteria>
  <done>Agent-facing guidance now points at the same boundary model as the new root README.</done>
</task>

<task type="auto">
  <name>Task 3: Add workspace-context guidance to `tiptop/README.md`</name>
  <files>tiptop/README.md</files>
  <read_first>tiptop/README.md, README.md, tiptop/docs/getting-started.md, tiptop/docs/development-build.md</read_first>
  <action>Add a short `## Workspace Context` section near the top of `tiptop/README.md`. State that when TiPToP is used in the validated local stack at `/home/user/tiptop`, the active system also includes sibling repos `sam3/`, `Fast-FoundationStereo/`, `M2T2`, and `droid-sim-evals/`, and tell readers to start from the workspace-root `README.md` for boundary and onboarding context before following the repo-local docs here.</action>
  <verify>bash -lc 'rg -n "## Workspace Context|/home/user/tiptop|sam3/|Fast-FoundationStereo/|M2T2|droid-sim-evals/|workspace-root README.md|workspace root README.md" tiptop/README.md'</verify>
  <acceptance_criteria>
    - `tiptop/README.md` contains `## Workspace Context`
    - `tiptop/README.md` contains `/home/user/tiptop`
    - `tiptop/README.md` contains `sam3/`, `Fast-FoundationStereo/`, `M2T2`, and `droid-sim-evals/`
    - `tiptop/README.md` tells readers to start from the workspace-root `README.md`
  </acceptance_criteria>
  <done>The TiPToP repo README now clearly fits inside the larger workspace instead of pretending to be the whole system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human reader -> root and repo READMEs | Contributors trust entrypoint docs to decide where to start and what belongs to the system |
| Agent guidance -> execution workflows | `AGENTS.md` influences future automated decisions about project scope and doc routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | S | `README.md`, `AGENTS.md`, and `tiptop/README.md` | mitigate | Use the exact same active baseline repo list and workspace-root wording across all three files so no file can spoof a different project boundary |
| T-01-05 | R | `README.md` deep-link routing | mitigate | Link readers to the specific existing TiPToP docs (`getting-started.md`, `development-build.md`, `command-reference.md`) instead of paraphrasing mutable command details |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bash -lc 'test -f README.md'`
- [ ] `bash -lc 'rg -n "/home/user/tiptop|sam3/|Fast-FoundationStereo/|M2T2|droid-sim-evals/" README.md tiptop/README.md >/dev/null'`
- [ ] `bash -lc 'rg -n "README.md|workspace root|multi-repo" AGENTS.md >/dev/null'`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Human and agent entrypoints now agree that the project starts at the workspace root
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-baseline/01-02-SUMMARY.md`
</output>
