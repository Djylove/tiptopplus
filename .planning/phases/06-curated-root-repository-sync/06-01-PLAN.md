---
phase: 06-curated-root-repository-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - WORKSPACE-SERVICES.md
  - WORKSPACE-BOOTSTRAP.md
  - .planning/codebase/WORKSPACE.md
  - .planning/codebase/CONCERNS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REPO-03
  - DOC-01
  - DOC-02
  - DOC-03
user_setup: []
must_haves:
  truths:
    - The curated root repo is documented as a workspace boundary, not as a self-contained runtime clone.
    - Collaborators can see which sibling repos must sit beside the root checkout.
    - Planning-side docs and roadmap routing reflect that the root repo is now git-tracked and ready for phase execution.
  artifacts:
    - README.md
    - WORKSPACE-SERVICES.md
    - WORKSPACE-BOOTSTRAP.md
    - .planning/codebase/WORKSPACE.md
    - .planning/codebase/CONCERNS.md
    - .planning/ROADMAP.md
  key_links:
    - `WORKSPACE-BOOTSTRAP.md` must link the curated root repo back to the existing `tiptop/docs/*` manuals rather than duplicating them.
    - `README.md`, `WORKSPACE-SERVICES.md`, and `.planning/codebase/WORKSPACE.md` must all say the same thing about excluded sibling repos and clone-alone limitations.
    - `.planning/ROADMAP.md` should route the next action to `$gsd-execute-phase 6` once planning artifacts exist.
---

<objective>
Make the curated root repository boundary explicit and collaborator-friendly.

Purpose: Close the documentation gap between “the root repo exists” and “another developer can understand and use it correctly.”
Output: A dedicated bootstrap doc plus aligned root/planning docs that explain the curated boundary and route the next workflow step correctly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-curated-root-repository-sync/06-CONTEXT.md
@.planning/phases/06-curated-root-repository-sync/06-RESEARCH.md
@.planning/phases/06-curated-root-repository-sync/06-VALIDATION.md
@.planning/phases/06-curated-root-repository-sync/06-PATTERNS.md
@README.md
@WORKSPACE-SERVICES.md
@.planning/codebase/WORKSPACE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `WORKSPACE-BOOTSTRAP.md` as the collaborator-facing curated-boundary contract</name>
  <files>WORKSPACE-BOOTSTRAP.md</files>
  <read_first>README.md, WORKSPACE-SERVICES.md, .planning/phases/06-curated-root-repository-sync/06-CONTEXT.md, .planning/phases/06-curated-root-repository-sync/06-RESEARCH.md, .planning/codebase/WORKSPACE.md</read_first>
  <action>Create `WORKSPACE-BOOTSTRAP.md` at the workspace root with these exact sections: `## What This Repo Contains`, `## Required Sibling Checkouts`, `## Expected Workspace Layout`, `## First Verification Pass`, `## What Is Intentionally Excluded`, and `## Where Detailed Runtime Docs Live`. In `## Required Sibling Checkouts`, list `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, and `droid-sim-evals/` as required siblings and `FoundationStereo/` as optional reference-only. In `## Expected Workspace Layout`, include a tree rooted at `/home/user/tiptop` showing the curated root repo beside the sibling repos. In `## First Verification Pass`, include `git remote -v`, `git branch --show-current`, and `bash scripts/verify_curated_workspace_repo.sh`. In `## What Is Intentionally Excluded`, name sibling repos, `.pixi/`, nested `.git` directories, and timestamped output folders as outside the curated upload boundary.</action>
  <verify>bash -lc 'test -f WORKSPACE-BOOTSTRAP.md && rg -n "## What This Repo Contains|## Required Sibling Checkouts|## Expected Workspace Layout|## First Verification Pass|## What Is Intentionally Excluded|sam3/|Fast-FoundationStereo/|M2T2/|droid-sim-evals/|FoundationStereo/" WORKSPACE-BOOTSTRAP.md'</verify>
  <acceptance_criteria>
    - `WORKSPACE-BOOTSTRAP.md` exists at the workspace root
    - `WORKSPACE-BOOTSTRAP.md` contains all six required section headings
    - `WORKSPACE-BOOTSTRAP.md` contains the exact sibling repo names `sam3/`, `Fast-FoundationStereo/`, `M2T2/`, `droid-sim-evals/`, and `FoundationStereo/`
    - `WORKSPACE-BOOTSTRAP.md` contains the exact command string `bash scripts/verify_curated_workspace_repo.sh`
  </acceptance_criteria>
  <done>A collaborator can open one root doc and understand the curated boundary, required siblings, expected layout, and first verification pass.</done>
</task>

<task type="auto">
  <name>Task 2: Link the new bootstrap contract from `README.md` and `WORKSPACE-SERVICES.md`</name>
  <files>README.md, WORKSPACE-SERVICES.md</files>
  <read_first>README.md, WORKSPACE-SERVICES.md, WORKSPACE-BOOTSTRAP.md, .planning/phases/06-curated-root-repository-sync/06-PATTERNS.md</read_first>
  <action>Update `README.md` and `WORKSPACE-SERVICES.md` so both explicitly link to `WORKSPACE-BOOTSTRAP.md` as the place to reconstruct the curated workspace layout. Keep the existing statement that cloning the root repo alone is not enough, and add one direct pointer from each file to the bootstrap doc without duplicating the full bootstrap instructions. In `README.md`, add the bootstrap doc to the `Start Here` or `Where To Go Next` navigation. In `WORKSPACE-SERVICES.md`, add one sentence near the top explaining that clone/layout expectations live in `WORKSPACE-BOOTSTRAP.md`.</action>
  <verify>bash -lc 'rg -n "WORKSPACE-BOOTSTRAP.md|Cloning this repo alone is therefore not enough|sibling repos" README.md WORKSPACE-SERVICES.md'</verify>
  <acceptance_criteria>
    - `README.md` contains `WORKSPACE-BOOTSTRAP.md`
    - `WORKSPACE-SERVICES.md` contains `WORKSPACE-BOOTSTRAP.md`
    - `README.md` still contains the exact sentence fragment `Cloning this repo alone is therefore not enough`
    - `WORKSPACE-SERVICES.md` contains the phrase `clone/layout expectations` or equivalent direct bootstrap wording tied to `WORKSPACE-BOOTSTRAP.md`
  </acceptance_criteria>
  <done>The root docs route collaborators into the new bootstrap contract without losing the current curated-boundary warning.</done>
</task>

<task type="auto">
  <name>Task 3: Align planning-side docs and roadmap routing with the git-tracked curated root repo</name>
  <files>.planning/codebase/WORKSPACE.md, .planning/codebase/CONCERNS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/codebase/WORKSPACE.md, .planning/codebase/CONCERNS.md, .planning/ROADMAP.md, .planning/phases/06-curated-root-repository-sync/06-CONTEXT.md, .planning/phases/06-curated-root-repository-sync/06-RESEARCH.md</read_first>
  <action>Update `.planning/codebase/WORKSPACE.md` so it explicitly states the root is now a curated root git repo that tracks `.planning/`, root docs, and `tiptop/` while leaving sibling repos external. Update `.planning/codebase/CONCERNS.md` so the top source-control concern says the root repo captures only a curated subset and that sibling repos remain external dependencies. Update `.planning/ROADMAP.md` so the `Next Step` section points to `$gsd-execute-phase 6` instead of `$gsd-plan-phase 6` now that planning artifacts exist.</action>
  <verify>bash -lc 'rg -n "curated root git repo|sibling repos remain external|curated subset|\\$gsd-execute-phase 6" .planning/codebase/WORKSPACE.md .planning/codebase/CONCERNS.md .planning/ROADMAP.md'</verify>
  <acceptance_criteria>
    - `.planning/codebase/WORKSPACE.md` contains the exact phrase `curated root git repo`
    - `.planning/codebase/WORKSPACE.md` contains the phrase `sibling repos remain external`
    - `.planning/codebase/CONCERNS.md` contains the phrase `curated subset`
    - `.planning/ROADMAP.md` contains the exact command `$gsd-execute-phase 6`
  </acceptance_criteria>
  <done>Planning-side source-of-truth docs match the new root-repo reality and route the next workflow step correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Collaborator -> root docs | New collaborators trust root docs to decide whether the curated repo is self-contained or requires sibling checkouts |
| Root docs -> planning docs | Workflow artifacts rely on planning-side and human-facing docs saying the same thing about the root repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | Root bootstrap and README guidance | mitigate | State explicitly that cloning the curated root repo alone is insufficient and list the required sibling repos by exact name |
| T-06-02 | I | Planning-side boundary docs | mitigate | Use the same curated-boundary language across `README.md`, `WORKSPACE-SERVICES.md`, `.planning/codebase/WORKSPACE.md`, and `.planning/codebase/CONCERNS.md` so future plans do not split into conflicting repo stories |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bash -lc 'test -f WORKSPACE-BOOTSTRAP.md'`
- [ ] `bash -lc 'rg -n "WORKSPACE-BOOTSTRAP.md|Cloning this repo alone is therefore not enough" README.md WORKSPACE-SERVICES.md >/dev/null'`
- [ ] `bash -lc 'rg -n "curated root git repo|sibling repos remain external|\\$gsd-execute-phase 6" .planning/codebase/WORKSPACE.md .planning/codebase/CONCERNS.md .planning/ROADMAP.md >/dev/null'`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No root or planning doc implies the curated repo is self-contained
- The next workflow step in `ROADMAP.md` is `$gsd-execute-phase 6`
</success_criteria>

<output>
After completion, create `.planning/phases/06-curated-root-repository-sync/06-01-SUMMARY.md`
</output>
