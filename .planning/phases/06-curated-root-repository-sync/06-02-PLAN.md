---
phase: 06-curated-root-repository-sync
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .gitignore
  - scripts/verify_curated_workspace_repo.sh
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
user_setup: []
must_haves:
  truths:
    - The root repo remains on `main` and tracks the `tiptopplus` origin.
    - The curated boundary continues excluding sibling repos, nested git histories, envs, and heavyweight output directories.
    - One rerunnable root-level command verifies both git state and representative ignore-boundary behavior.
  artifacts:
    - .gitignore
    - scripts/verify_curated_workspace_repo.sh
  key_links:
    - `scripts/verify_curated_workspace_repo.sh` should validate the same curated-boundary story described by `README.md` and `WORKSPACE-BOOTSTRAP.md`.
    - Representative `git check-ignore` assertions must cover both root-level sibling repos and nested heavyweight paths inside `tiptop/`.
---

<objective>
Lock down the curated upload boundary and give the root repo one explicit verification entrypoint.

Purpose: Replace one-time manual checking with a repeatable repo-boundary audit that proves the curated root repo is still safe to maintain and sync.
Output: A root verification script plus an audited `.gitignore` boundary for representative heavyweight paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-curated-root-repository-sync/06-CONTEXT.md
@.planning/phases/06-curated-root-repository-sync/06-RESEARCH.md
@.planning/phases/06-curated-root-repository-sync/06-VALIDATION.md
@.planning/phases/06-curated-root-repository-sync/06-PATTERNS.md
@.gitignore
@README.md
@WORKSPACE-SERVICES.md
@tiptop/install/install-curobo.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `scripts/verify_curated_workspace_repo.sh` as the root repo audit entrypoint</name>
  <files>scripts/verify_curated_workspace_repo.sh</files>
  <read_first>.gitignore, README.md, WORKSPACE-SERVICES.md, WORKSPACE-BOOTSTRAP.md, tiptop/install/install-curobo.sh, .planning/phases/06-curated-root-repository-sync/06-PATTERNS.md</read_first>
  <action>Create `scripts/verify_curated_workspace_repo.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. The script must run from the workspace root and fail if `git rev-parse --is-inside-work-tree` is false. It must require `git branch --show-current` to equal `main`. It must read `origin` via `git remote get-url origin` and check it against `${TIPTOPPLUS_EXPECTED_REMOTE_REGEX:-Djylove/tiptopplus(\\.git)?$}`. It must verify the existence of `README.md`, `WORKSPACE-SERVICES.md`, `.planning/PROJECT.md`, and `WORKSPACE-BOOTSTRAP.md`. It must run `git check-ignore` on these representative paths: `sam3`, `Fast-FoundationStereo`, `FoundationStereo`, `M2T2`, `droid-sim-evals`, `tiptop/curobo`, `tiptop/cutamp`, `tiptop/.pixi`, `tiptop/tiptop_h5_scene4_capfix3`, `tiptop/tiptop_server_outputs`, and `tmp_scene4_frames`. The script should print clear `PASS:` lines and finish with the exact line `Curated workspace repo verification passed`.</action>
  <verify>bash -lc 'bash scripts/verify_curated_workspace_repo.sh'</verify>
  <acceptance_criteria>
    - `scripts/verify_curated_workspace_repo.sh` exists
    - `scripts/verify_curated_workspace_repo.sh` contains `set -euo pipefail`
    - `scripts/verify_curated_workspace_repo.sh` contains the exact variable name `TIPTOPPLUS_EXPECTED_REMOTE_REGEX`
    - Running `bash scripts/verify_curated_workspace_repo.sh` exits 0 and prints `Curated workspace repo verification passed`
  </acceptance_criteria>
  <done>The root repo has one rerunnable verification surface for branch, remote, key docs, and representative ignored paths.</done>
</task>

<task type="auto">
  <name>Task 2: Audit `.gitignore` against representative heavyweight and nested-repo paths</name>
  <files>.gitignore</files>
  <read_first>.gitignore, .planning/phases/06-curated-root-repository-sync/06-CONTEXT.md, .planning/phases/06-curated-root-repository-sync/06-RESEARCH.md, scripts/verify_curated_workspace_repo.sh</read_first>
  <action>Audit `.gitignore` so it explicitly keeps `.planning/**` while excluding the heavyweight sibling repos `sam3/`, `Fast-FoundationStereo/`, `FoundationStereo/`, `M2T2/`, and `droid-sim-evals/`; root artifacts `SAM3`, `*.zip`, `d435_probe_outputs/`, `sam3_*_outputs/`, `sam3_scene4_perception_check/`, and `tmp_scene4_frames/`; nested repos `tiptop/curobo/`, `tiptop/cutamp/`, and `**/.git`; environment/cache directories such as `tiptop/.pixi/`, `tiptop/.pytest_cache/`, `tiptop/.cache/`, and `tiptop/tiptop.egg-info/`; and representative timestamped output folders such as `tiptop/tiptop_h5_*/`, `tiptop/tiptop_server_outputs/`, `tiptop/tiptop_urinal_outputs/`, and `tiptop/milestone1_visuals_2026-04-21/`. Preserve the grouped comment structure so future maintainers can tell why each bucket is excluded.</action>
  <verify>bash -lc 'git check-ignore sam3 Fast-FoundationStereo FoundationStereo M2T2 droid-sim-evals tiptop/curobo tiptop/cutamp tiptop/.pixi tiptop/tiptop_h5_scene4_capfix3 tiptop/tiptop_server_outputs tmp_scene4_frames assets.zip >/dev/null && rg -n "^sam3/$|^Fast-FoundationStereo/$|^FoundationStereo/$|^M2T2/$|^droid-sim-evals/$|^tiptop/curobo/$|^tiptop/cutamp/$|^tiptop/.pixi/$|^tiptop/tiptop_h5_\\*/$|^tiptop/tiptop_server_outputs/$|^\\*\\*/.git$" .gitignore'</verify>
  <acceptance_criteria>
    - `.gitignore` contains the exact lines `sam3/`, `Fast-FoundationStereo/`, `FoundationStereo/`, `M2T2/`, and `droid-sim-evals/`
    - `.gitignore` contains the exact lines `tiptop/curobo/`, `tiptop/cutamp/`, and `**/.git`
    - `.gitignore` contains the exact lines `tiptop/.pixi/`, `tiptop/tiptop_h5_*/`, and `tiptop/tiptop_server_outputs/`
    - `git check-ignore` succeeds for all representative heavyweight paths named in the verify command
  </acceptance_criteria>
  <done>The curated upload boundary is explicit, grouped, and proven against representative heavyweight paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer -> `.gitignore` | Maintainers trust ignore rules to prevent accidental staging of heavyweight or nested-repo content |
| Maintainer -> root verification script | Maintainers trust the verification script to represent the intended git and ignore boundary accurately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T | `scripts/verify_curated_workspace_repo.sh` remote and branch checks | mitigate | Require exact `main` branch, validate `origin` against a configurable regex, and fail loudly on missing root docs |
| T-06-04 | I | `.gitignore` curated-boundary rules | mitigate | Keep grouped exclusions explicit and verify representative heavyweight paths with `git check-ignore` instead of trusting comments alone |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bash -lc 'bash scripts/verify_curated_workspace_repo.sh'`
- [ ] `bash -lc 'git check-ignore sam3 Fast-FoundationStereo FoundationStereo M2T2 droid-sim-evals tiptop/curobo tiptop/cutamp tiptop/.pixi tiptop/tiptop_h5_scene4_capfix3 tiptop/tiptop_server_outputs tmp_scene4_frames assets.zip >/dev/null'`
- [ ] `bash -lc 'rg -n "TIPTOPPLUS_EXPECTED_REMOTE_REGEX|Curated workspace repo verification passed" scripts/verify_curated_workspace_repo.sh >/dev/null'`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The root repo has a single rerunnable repo-boundary verification command
- Representative heavyweight and nested-repo paths remain ignored by the curated root repo
</success_criteria>

<output>
After completion, create `.planning/phases/06-curated-root-repository-sync/06-02-SUMMARY.md`
</output>
